name: Test
on: [push]
jobs:
  Github-Projects:
    runs-on: ubuntu-latest
    steps:
      - name: Hola mundo
        run: echo hola mundo
      - name: Repositorio
        uses: actions/checkout@v7
      - run: ls -la
      - name: Setup node
        uses: actions/setup-node@v6
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
